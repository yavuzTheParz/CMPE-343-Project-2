/**
 * Defines the different types of entities that can exist in the system.
 * This enum is used to distinguish between Contact objects and User objects
 * when performing operations such as serialization, database handling, or UI logic.
 */
public enum EntityType
{
    CONTACT,
    USER
}